import * as greeting from './greeting'
import * as prompt from './prompt'
import * as workflow from './workflow'
import * as serialize from './serialize'
import * as initOpenRouter from './init-open-router'
import * as chooseWorkflow from './choose-workflow'

export const commands = [greeting, prompt, workflow, serialize, initOpenRouter, chooseWorkflow]
